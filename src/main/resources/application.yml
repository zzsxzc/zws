server:
  port: 443
  ssl:
    key-store: freedommxc.top.jks
    key-store-password: q7f874uh7g211
    key-store-type: JKS
#    key-alias: freedommxc

spring:
  datasource:
    username: root
    #password: 123456
    password: jtwmydtsgx@666
    url: jdbc:mysql://localhost:3306/zws?serverTimezone=Asia/Shanghai
    driver-class-name: com.mysql.cj.jdbc.Driver
    type: com.alibaba.druid.pool.DruidDataSource

mybatis:
  # 指定全局配置文件位置
  config-location: classpath:mybatis/mybatis-config.xml
  # 指定sql映射文件位置
  mapper-locations: classpath:mybatis/mapper/*.xml
  #mapper-locations: mybatis/mapper/*.xml